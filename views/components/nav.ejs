<nav class="site-nav">
    <a href="./">Dashboard</a>
    <% if (typeof permissions !== 'undefined' && permissions.includes('view:alerts')) { %>
      <a href="alerts">Alerts</a>
    <% } %>
    <% if (typeof permissions !== 'undefined' && permissions.includes('view:reports')) { %>
      <a href="reports">Reports</a>
    <% } %>
    <% if (typeof permissions !== 'undefined' && permissions.includes('view:logs')) { %>
      <a href="logs">Logs</a>
    <% } %>

    <% if (typeof permissions !== 'undefined' && permissions.includes('manage:users')) { %>
      <a href="/admin/users">User Management</a>
    <% } %>
</nav>