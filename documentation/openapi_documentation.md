- Open API is a standard for documenting and API and its REST endpoints.
	- This means that we can write the API specification, then use tools like Postman to automatically generate / run tests against the API.
- We will define the API specification in the `documentation/openapi.yml` file, which uses YAML syntax.

- You can view the docs in your browser at `[Domain]/docs`